# test-time requirements

## note: if needed to specify arch, this is the syntax:
# PyPartMC==0.0.9; platform_machine == 'x86_64'

matplotlib==3.5.3
jupyter-core<5.0.0
ipywidgets!=8.0.3
ghapi
pytest
pytest-timeout

# note: if cloning both PySDM and PySDM examples, consider "pip install -e"
PySDM-examples @ git+https://github.com/slayoo/PySDM-examples@13a5fe3#egg=PySDM-examples
PyMPDATA @ git+https://github.com/open-atmos/PyMPDATA@63f1c11#egg=PyMPDATA

PyPartMC==0.0.32
