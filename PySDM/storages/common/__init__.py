"""
The package contains base classes and utility functions required to implement the storages.

The package contains the following modules:

backend

: the backend base classes

common

: the common base classes

storage

: the storage base classes


pair_indicator

: the pair indicator storage

random

: the random number generator storage


index

: the index storage


indexed

: the indexed storage

utils

: the utility functions
"""
