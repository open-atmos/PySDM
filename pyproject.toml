[tool.isort]
profile = "black"

[tool.setuptools_scm]
local_scheme = "PySDM.impl.scm_version:scheme"
version_scheme = "post-release"

[build-system]
requires = ['setuptools==75.3.0', 'setuptools-scm==8.1.0']
